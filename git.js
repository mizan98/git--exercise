console.log("My name is Mizan")

/////////////////////////////GIT/////////////////////////////

//git allows us to track our activity to track it 

//use git add and then the name of the file to track a file 

//use git status after track to see if the file has ben modified 

//to create a save with git do: git commit -m " whatever i wanted to save"

//to set up your own name as the auther and emaim address as you do the following: git config --global user.name "Mizan"
                                                                                // git config --global user.email mizan_98@hotmail.co.uk

//to push files to github do :git push -u origin master

let name = "mizan Choudhury"

console.log(`this is a test to practice using git with diofferent changes ${name}`)